<!DOCTYPE html>
<html>
<head>
  <title>Rule Versioning</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }
    .container { max-width: 1200px; margin: 0 auto; }
    .header {
      background: white;
      padding: 30px;
      border-radius: 15px;
      margin-bottom: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
    .rules-list {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
    .rule-item {
      padding: 15px;
      border-bottom: 1px solid #eee;
    }
    .rule-item:hover {
      background: #f8f9fa;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🔄 Rule Version Management</h1>
      <p>Manage versions, activate, and rollback rules</p>
      <a href="/demo/test_center" style="color: #667eea;">← Back</a>
    </div>

    <div class="rules-list">
      <h2>All Rules</h2>
      <% if @rules.any? %>
        <% @rules.each do |rule| %>
          <div class="rule-item">
            <h3><%= rule.rule_id %></h3>
            <p><%= rule.description %></p>
            <p style="color: #666; font-size: 0.9em;">
              Status: <strong><%= rule.status %></strong> |
              Versions: <%= rule.versions.count %>
            </p>
          </div>
        <% end %>
      <% else %>
        <p>No rules found. Please seed the database first.</p>
        <a href="/demo/seed_all_data" style="display: inline-block; background: #667eea; color: white; padding: 12px 24px; border-radius: 8px; text-decoration: none; margin-top: 15px;">
          Seed Data
        </a>
      <% end %>
    </div>
  </div>
</body>
</html>
